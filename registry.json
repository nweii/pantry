{
  "components": {
    "grain": {
      "description": "A procedural SVG grain/noise texture overlay component for React. Renders as an absolutely-positioned overlay using SVG feTurbulence filters — no external dependencies beyond React.",
      "files": [
        "src/grain.tsx"
      ],
      "dependencies": []
    }
  }
}
